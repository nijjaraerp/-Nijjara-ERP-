# 🧩 SYS MODULE — System Users, Roles, Permissions, Audit

scss
Copy code
       ┌────────────────────┐
       │  SYS Module (Admin)│
       └────────┬───────────┘
                │
                ▼
┌──────────────────────────────────────────────┐
│ SYS_Users.html (Frontend: manage users)      │
│ SYS_Roles.html (Frontend: assign roles)      │
│ SYS_AuditLog.html (Frontend: view logs)      │
└────────┬───────────────────────────┬─────────┘
         │                           │
         ▼                           ▼
┌──────────────────────┐ ┌──────────────────────────────┐
│ SYS_Users.js │ │ Logging.js │
│ 🛠️ Create/Update/Delete │ │ logInfo_, logError_ → Audit Log │
└────────┬─────────────┘ └────────────┬─────────────────┘
│ │
▼ ▼
┌──────────────────────────────┐ ┌────────────────────────────┐
│ [SYS_Users] [Sheet] │ │ [SYS_Audit_Log] [Sheet] │
│ [PK: USERNAME] │ │ Appends every write action │
└──────────────────────────────┘ └────────────────────────────┘

diff
Copy code
Additional Linked Sheets:
- SYS_Roles         (static roles)
- SYS_Permissions   (seeded list)
- SYS_Sessions      (active session tracking)
🔐 Access Gated By:

Permissions like SYS.Users.Create, SYS.Users.Delete, etc.

Copy code
