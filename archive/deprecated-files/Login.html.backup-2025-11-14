<!doctype html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Nijjara ERP - Quantum Access Portal</title>
    <style>
      /* Futuristic theme variables - Year 3025 */
      :root {
        --void-black: #000000;
        --deep-space: #0a0e27;
        --quantum-blue: #00d9ff;
        --cyber-purple: #b030ff;
        --neon-pink: #ff006e;
        --plasma-teal: #00ffc8;
        --energy-yellow: #ffd700;
        --hologram-green: #39ff14;
        --neural-violet: #8b00ff;
        --shadow-overlay: rgba(10, 14, 39, 0.95);
        --glow-intensity: rgba(0, 217, 255, 0.4);
        --pulse-glow: rgba(176, 48, 255, 0.3);
        --success-glow: rgba(57, 255, 20, 0.3);
        --error-glow: rgba(255, 0, 110, 0.3);
        --glass-border: rgba(255, 255, 255, 0.1);
        --glass-bg: rgba(255, 255, 255, 0.03);
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family:
          'Orbitron',
          'Rajdhani',
          'Exo 2',
          -apple-system,
          BlinkMacSystemFont,
          sans-serif;
        background: radial-gradient(
          ellipse at center,
          var(--deep-space) 0%,
          var(--void-black) 100%
        );
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 20px;
        position: relative;
        overflow: hidden;
        color: #ffffff;
      }

      /* Animated starfield background */
      .starfield {
        position: fixed;
        inset: 0;
        z-index: 0;
        pointer-events: none;
        background-image:
          radial-gradient(2px 2px at 20px 30px, white, rgba(255, 255, 255, 0)),
          radial-gradient(2px 2px at 60px 70px, white, rgba(255, 255, 255, 0)),
          radial-gradient(1px 1px at 50px 50px, white, rgba(255, 255, 255, 0)),
          radial-gradient(1px 1px at 130px 80px, white, rgba(255, 255, 255, 0)),
          radial-gradient(2px 2px at 90px 10px, white, rgba(255, 255, 255, 0));
        background-repeat: repeat;
        background-size: 200px 200px;
        animation: starsMove 120s linear infinite;
        opacity: 0.5;
      }

      @keyframes starsMove {
        from {
          transform: translateY(0);
        }
        to {
          transform: translateY(200px);
        }
      }

      /* Particle canvas */
      #particleCanvas {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: 1;
        pointer-events: none;
      }

      /* Neural network grid */
      .neural-grid {
        position: fixed;
        inset: 0;
        z-index: 1;
        pointer-events: none;
        background-image:
          linear-gradient(var(--glass-border) 1px, transparent 1px),
          linear-gradient(90deg, var(--glass-border) 1px, transparent 1px);
        background-size: 50px 50px;
        animation: gridPulse 8s ease-in-out infinite;
        opacity: 0.1;
      }

      @keyframes gridPulse {
        0%,
        100% {
          opacity: 0.05;
          transform: scale(1);
        }
        50% {
          opacity: 0.15;
          transform: scale(1.02);
        }
      }

      /* Holographic scanning lines */
      .scan-effect {
        position: fixed;
        inset: 0;
        z-index: 2;
        pointer-events: none;
        background: linear-gradient(
          0deg,
          transparent 0%,
          var(--quantum-blue) 50%,
          transparent 100%
        );
        height: 100px;
        opacity: 0.03;
        animation: scanVertical 6s linear infinite;
      }

      @keyframes scanVertical {
        0% {
          transform: translateY(-100vh);
        }
        100% {
          transform: translateY(100vh);
        }
      }

      /* Floating orbs */
      .orb {
        position: fixed;
        border-radius: 50%;
        filter: blur(60px);
        opacity: 0.4;
        z-index: 0;
        pointer-events: none;
        animation: floatOrb 20s ease-in-out infinite;
      }

      .orb-1 {
        top: 10%;
        left: 20%;
        width: 300px;
        height: 300px;
        background: radial-gradient(circle, var(--quantum-blue), transparent);
        animation-delay: 0s;
      }

      .orb-2 {
        bottom: 20%;
        right: 15%;
        width: 250px;
        height: 250px;
        background: radial-gradient(circle, var(--cyber-purple), transparent);
        animation-delay: 5s;
      }

      .orb-3 {
        top: 50%;
        right: 30%;
        width: 200px;
        height: 200px;
        background: radial-gradient(circle, var(--plasma-teal), transparent);
        animation-delay: 10s;
      }

      @keyframes floatOrb {
        0%,
        100% {
          transform: translate(0, 0) scale(1);
        }
        33% {
          transform: translate(30px, -50px) scale(1.1);
        }
        66% {
          transform: translate(-30px, 50px) scale(0.9);
        }
      }

      .login-container {
        width: 100%;
        max-width: 520px;
        position: relative;
        z-index: 10;
        animation: containerFloat 6s ease-in-out infinite;
      }

      @keyframes containerFloat {
        0%,
        100% {
          transform: translateY(0px);
        }
        50% {
          transform: translateY(-10px);
        }
      }

      /* Holographic card with glassmorphism */
      .quantum-card {
        position: relative;
        background: linear-gradient(
          135deg,
          rgba(255, 255, 255, 0.05) 0%,
          rgba(255, 255, 255, 0.02) 100%
        );
        backdrop-filter: blur(20px);
        -webkit-backdrop-filter: blur(20px);
        border-radius: 24px;
        overflow: hidden;
        border: 2px solid;
        border-image: linear-gradient(
            135deg,
            var(--quantum-blue),
            var(--cyber-purple),
            var(--plasma-teal)
          )
          1;
        box-shadow:
          0 8px 32px 0 rgba(0, 217, 255, 0.2),
          0 0 60px 0 var(--pulse-glow),
          inset 0 0 40px 0 rgba(255, 255, 255, 0.03);
        animation: cardGlow 4s ease-in-out infinite;
      }

      @keyframes cardGlow {
        0%,
        100% {
          box-shadow:
            0 8px 32px 0 rgba(0, 217, 255, 0.2),
            0 0 60px 0 var(--pulse-glow),
            inset 0 0 40px 0 rgba(255, 255, 255, 0.03);
        }
        50% {
          box-shadow:
            0 8px 32px 0 rgba(176, 48, 255, 0.3),
            0 0 80px 0 var(--glow-intensity),
            inset 0 0 40px 0 rgba(255, 255, 255, 0.05);
        }
      }

      /* Animated border glow */
      .quantum-card::before {
        content: '';
        position: absolute;
        inset: -2px;
        background: linear-gradient(
          45deg,
          var(--quantum-blue),
          var(--cyber-purple),
          var(--plasma-teal),
          var(--neon-pink),
          var(--quantum-blue)
        );
        background-size: 300% 300%;
        border-radius: 24px;
        z-index: -1;
        animation: borderGlow 6s linear infinite;
        filter: blur(8px);
        opacity: 0.6;
      }

      @keyframes borderGlow {
        0% {
          background-position: 0% 50%;
        }
        50% {
          background-position: 100% 50%;
        }
        100% {
          background-position: 0% 50%;
        }
      }

      /* Holographic reflection effect */
      .quantum-card::after {
        content: '';
        position: absolute;
        top: -50%;
        left: -50%;
        width: 200%;
        height: 200%;
        background: linear-gradient(
          45deg,
          transparent 30%,
          rgba(255, 255, 255, 0.1) 50%,
          transparent 70%
        );
        transform: rotate(45deg);
        animation: holoReflection 8s linear infinite;
      }

      @keyframes holoReflection {
        0% {
          transform: translateX(-100%) rotate(45deg);
        }
        100% {
          transform: translateX(100%) rotate(45deg);
        }
      }

      /* Header with quantum effects */
      .quantum-header {
        position: relative;
        padding: 40px 40px 20px;
        text-align: center;
        z-index: 1;
      }

      .quantum-logo {
        width: 80px;
        height: 80px;
        margin: 0 auto 20px;
        position: relative;
        animation: logoSpin 20s linear infinite;
      }

      @keyframes logoSpin {
        0% {
          transform: rotate(0deg);
        }
        100% {
          transform: rotate(360deg);
        }
      }

      .logo-ring {
        position: absolute;
        inset: 0;
        border-radius: 50%;
        border: 2px solid var(--quantum-blue);
        animation: ringPulse 2s ease-in-out infinite;
      }

      .logo-ring:nth-child(2) {
        animation-delay: 0.3s;
        border-color: var(--cyber-purple);
      }

      .logo-ring:nth-child(3) {
        animation-delay: 0.6s;
        border-color: var(--plasma-teal);
      }

      @keyframes ringPulse {
        0%,
        100% {
          transform: scale(1);
          opacity: 1;
          box-shadow: 0 0 20px currentColor;
        }
        50% {
          transform: scale(1.2);
          opacity: 0.5;
          box-shadow: 0 0 40px currentColor;
        }
      }

      .logo-core {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        width: 40px;
        height: 40px;
        background: linear-gradient(135deg, var(--quantum-blue), var(--cyber-purple));
        border-radius: 50%;
        box-shadow:
          0 0 30px var(--quantum-blue),
          inset 0 0 20px rgba(255, 255, 255, 0.5);
        animation: corePulse 3s ease-in-out infinite;
      }

      @keyframes corePulse {
        0%,
        100% {
          transform: translate(-50%, -50%) scale(1);
        }
        50% {
          transform: translate(-50%, -50%) scale(1.1);
        }
      }

      .quantum-title {
        font-size: 2.5rem;
        font-weight: 900;
        text-transform: uppercase;
        letter-spacing: 4px;
        margin-bottom: 10px;
        background: linear-gradient(
          90deg,
          var(--quantum-blue),
          var(--cyber-purple),
          var(--plasma-teal),
          var(--quantum-blue)
        );
        background-size: 200% auto;
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        animation: titleGradient 3s linear infinite;
        text-shadow: 0 0 30px var(--glow-intensity);
        position: relative;
      }

      @keyframes titleGradient {
        0% {
          background-position: 0% center;
        }
        100% {
          background-position: 200% center;
        }
      }

      .quantum-subtitle {
        font-size: 0.85rem;
        letter-spacing: 3px;
        color: var(--plasma-teal);
        text-transform: uppercase;
        opacity: 0.9;
        font-weight: 300;
        text-shadow: 0 0 10px var(--plasma-teal);
      }

      .access-code {
        display: inline-block;
        margin-top: 5px;
        padding: 4px 12px;
        background: rgba(0, 217, 255, 0.1);
        border: 1px solid var(--quantum-blue);
        border-radius: 20px;
        font-size: 0.7rem;
        color: var(--quantum-blue);
        letter-spacing: 2px;
        animation: codeBlink 2s ease-in-out infinite;
      }

      @keyframes codeBlink {
        0%,
        100% {
          opacity: 1;
        }
        50% {
          opacity: 0.6;
        }
      }

      /* Form content area */
      .quantum-content {
        padding: 0 40px 40px;
        position: relative;
        z-index: 1;
      }

      .neon-form .input-group {
        margin-bottom: 28px;
        position: relative;
      }

      /* Holographic input field */
      .holo-input-wrapper {
        position: relative;
      }

      /* Corner decorations */
      .holo-corners {
        position: absolute;
        inset: -8px;
        pointer-events: none;
      }

      .corner {
        position: absolute;
        width: 20px;
        height: 20px;
        border: 2px solid var(--quantum-blue);
        opacity: 0.5;
        transition: all 0.3s ease;
      }

      .corner-tl {
        top: 0;
        left: 0;
        border-right: none;
        border-bottom: none;
      }

      .corner-tr {
        top: 0;
        right: 0;
        border-left: none;
        border-bottom: none;
      }

      .corner-bl {
        bottom: 0;
        left: 0;
        border-right: none;
        border-top: none;
      }

      .corner-br {
        bottom: 0;
        right: 0;
        border-left: none;
        border-top: none;
      }

      .holo-input-wrapper:focus-within .corner {
        opacity: 1;
        box-shadow: 0 0 10px var(--quantum-blue);
        animation: cornerPulse 1s ease-in-out infinite;
      }

      @keyframes cornerPulse {
        0%,
        100% {
          transform: scale(1);
        }
        50% {
          transform: scale(1.1);
        }
      }

      .neon-form input {
        width: 100%;
        background: rgba(0, 0, 0, 0.5);
        color: #ffffff;
        border: 2px solid rgba(0, 217, 255, 0.3);
        padding: 16px 20px;
        border-radius: 12px;
        outline: none;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        font-size: 15px;
        font-family: 'Rajdhani', sans-serif;
        letter-spacing: 1px;
        position: relative;
        z-index: 1;
        backdrop-filter: blur(10px);
      }

      .neon-form input::placeholder {
        color: rgba(255, 255, 255, 0.3);
        font-weight: 300;
      }

      .neon-form input:focus {
        background: rgba(0, 217, 255, 0.05);
        border-color: var(--quantum-blue);
        box-shadow:
          0 0 20px rgba(0, 217, 255, 0.3),
          inset 0 0 20px rgba(0, 217, 255, 0.1),
          0 4px 20px rgba(0, 0, 0, 0.3);
        transform: translateY(-2px);
      }

      .neon-form input:hover:not(:focus) {
        border-color: rgba(0, 217, 255, 0.5);
        background: rgba(0, 217, 255, 0.02);
        box-shadow: 0 0 10px rgba(0, 217, 255, 0.2);
      }

      /* Floating label */
      .neon-form label {
        position: absolute;
        left: 20px;
        top: -12px;
        background: linear-gradient(90deg, var(--deep-space), var(--deep-space));
        color: var(--quantum-blue);
        font-size: 0.75rem;
        padding: 0 8px;
        letter-spacing: 2px;
        font-weight: 600;
        text-transform: uppercase;
        transition: all 0.3s ease;
        z-index: 2;
        text-shadow: 0 0 10px var(--quantum-blue);
      }

      .holo-input-wrapper:focus-within label {
        color: var(--plasma-teal);
        text-shadow: 0 0 15px var(--plasma-teal);
        transform: translateY(-2px);
      }

      /* Scan line effect on input */
      .scan-line-input {
        position: absolute;
        bottom: 0;
        left: 0;
        width: 0;
        height: 2px;
        background: linear-gradient(
          90deg,
          transparent,
          var(--quantum-blue),
          var(--plasma-teal),
          transparent
        );
        box-shadow: 0 0 10px var(--quantum-blue);
        transition: width 0.4s ease;
        z-index: 2;
        border-radius: 2px;
      }

      .holo-input-wrapper:focus-within .scan-line-input {
        width: 100%;
      }

      /* Particle trail effect */
      .input-particles {
        position: absolute;
        inset: 0;
        pointer-events: none;
        overflow: hidden;
        border-radius: 12px;
      }

      .particle {
        position: absolute;
        width: 3px;
        height: 3px;
        background: var(--quantum-blue);
        border-radius: 50%;
        opacity: 0;
        box-shadow: 0 0 8px var(--quantum-blue);
      }

      .holo-input-wrapper:focus-within .particle {
        animation: particleFloat 2s ease-in-out infinite;
      }

      @keyframes particleFloat {
        0% {
          transform: translate(0, 0);
          opacity: 0;
        }
        10% {
          opacity: 1;
        }
        90% {
          opacity: 1;
        }
        100% {
          transform: translate(var(--tx), var(--ty));
          opacity: 0;
        }
      }

      /* Options row */
      .quantum-options {
        display: flex;
        align-items: center;
        justify-content: space-between;
        margin: 20px 0 30px;
      }

      .holo-checkbox {
        display: flex;
        align-items: center;
        gap: 12px;
        color: var(--quantum-blue);
        font-size: 0.85rem;
        cursor: pointer;
        transition: all 0.3s ease;
        letter-spacing: 1px;
      }

      .holo-checkbox input[type='checkbox'] {
        appearance: none;
        width: 20px;
        height: 20px;
        border: 2px solid var(--quantum-blue);
        border-radius: 4px;
        background: rgba(0, 0, 0, 0.5);
        cursor: pointer;
        position: relative;
        transition: all 0.3s ease;
      }

      .holo-checkbox input[type='checkbox']:checked {
        background: linear-gradient(135deg, var(--quantum-blue), var(--cyber-purple));
        box-shadow: 0 0 15px var(--quantum-blue);
      }

      .holo-checkbox input[type='checkbox']:checked::before {
        content: '✓';
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        color: #fff;
        font-size: 14px;
        font-weight: bold;
      }

      .holo-checkbox:hover input[type='checkbox'] {
        box-shadow: 0 0 10px var(--quantum-blue);
        transform: scale(1.05);
      }

      .holo-checkbox:hover span {
        color: var(--plasma-teal);
        text-shadow: 0 0 10px var(--plasma-teal);
      }

      .quantum-options a {
        color: var(--neon-pink);
        font-size: 0.85rem;
        text-decoration: none;
        transition: all 0.3s ease;
        cursor: pointer;
        letter-spacing: 1px;
        text-shadow: 0 0 5px var(--neon-pink);
      }

      .quantum-options a:hover {
        color: var(--energy-yellow);
        text-shadow: 0 0 15px var(--energy-yellow);
        transform: translateY(-2px);
      }

      /* Quantum login button */
      .quantum-button-wrapper {
        position: relative;
        margin-top: 10px;
      }

      #loginBtn {
        width: 100%;
        padding: 18px 24px;
        border: none;
        background: linear-gradient(135deg, var(--quantum-blue), var(--cyber-purple));
        color: #ffffff;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 3px;
        border-radius: 12px;
        cursor: pointer;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        position: relative;
        overflow: hidden;
        font-size: 16px;
        box-shadow:
          0 0 30px rgba(0, 217, 255, 0.4),
          0 4px 20px rgba(0, 0, 0, 0.3);
      }

      #loginBtn::before {
        content: '';
        position: absolute;
        top: 50%;
        left: 50%;
        width: 0;
        height: 0;
        background: radial-gradient(circle, rgba(255, 255, 255, 0.3), transparent);
        transform: translate(-50%, -50%);
        transition:
          width 0.6s,
          height 0.6s;
        border-radius: 50%;
      }

      #loginBtn:hover::before {
        width: 300%;
        height: 300%;
      }

      #loginBtn:hover {
        box-shadow:
          0 0 50px rgba(0, 217, 255, 0.6),
          0 0 30px rgba(176, 48, 255, 0.4),
          0 8px 30px rgba(0, 0, 0, 0.4);
        transform: translateY(-3px);
        letter-spacing: 4px;
      }

      #loginBtn:active:not(:disabled) {
        transform: translateY(-1px);
        box-shadow:
          0 0 30px rgba(0, 217, 255, 0.4),
          0 2px 15px rgba(0, 0, 0, 0.3);
      }

      #loginBtn:disabled {
        opacity: 0.6;
        cursor: not-allowed;
        transform: none;
      }

      /* Loading spinner */
      .button-spinner {
        display: none;
        width: 20px;
        height: 20px;
        border: 3px solid rgba(255, 255, 255, 0.3);
        border-top: 3px solid #ffffff;
        border-radius: 50%;
        animation: spin 0.8s linear infinite;
        margin: 0 auto;
      }

      #loginBtn.loading .button-spinner {
        display: block;
      }

      #loginBtn.loading .button-text {
        display: none;
      }

      @keyframes spin {
        0% {
          transform: rotate(0deg);
        }
        100% {
          transform: rotate(360deg);
        }
      }

      /* Messages */
      .message {
        margin-top: 20px;
        padding: 14px 18px;
        border-radius: 12px;
        font-size: 0.9rem;
        display: none;
        text-align: center;
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        opacity: 0;
        transform: translateY(-10px);
        letter-spacing: 0.5px;
        backdrop-filter: blur(10px);
      }

      .message.show {
        display: block;
        opacity: 1;
        transform: translateY(0);
        animation: messageSlide 0.4s ease-out;
      }

      @keyframes messageSlide {
        from {
          transform: translateY(-20px);
          opacity: 0;
        }
        to {
          transform: translateY(0);
          opacity: 1;
        }
      }

      .error {
        background: rgba(255, 0, 110, 0.1);
        border: 2px solid var(--neon-pink);
        color: var(--neon-pink);
        box-shadow: 0 0 20px var(--error-glow);
        animation: errorPulse 1s ease-in-out;
      }

      @keyframes errorPulse {
        0%,
        100% {
          box-shadow: 0 0 20px var(--error-glow);
        }
        50% {
          box-shadow: 0 0 40px var(--error-glow);
        }
      }

      .success {
        background: rgba(57, 255, 20, 0.1);
        border: 2px solid var(--hologram-green);
        color: var(--hologram-green);
        box-shadow: 0 0 20px var(--success-glow);
        animation: successPulse 1s ease-in-out;
      }

      @keyframes successPulse {
        0%,
        100% {
          box-shadow: 0 0 20px var(--success-glow);
        }
        50% {
          box-shadow: 0 0 40px var(--success-glow);
        }
      }

      /* Footer */
      .quantum-footer {
        text-align: center;
        margin-top: 30px;
        color: rgba(255, 255, 255, 0.4);
        font-size: 0.75rem;
        letter-spacing: 2px;
        animation: footerPulse 3s ease-in-out infinite;
      }

      @keyframes footerPulse {
        0%,
        100% {
          opacity: 0.4;
        }
        50% {
          opacity: 0.7;
        }
      }

      /* Loading overlay for authentication */
      .auth-overlay {
        position: fixed;
        inset: 0;
        background: rgba(0, 0, 0, 0.9);
        z-index: 9999;
        display: none;
        align-items: center;
        justify-content: center;
        backdrop-filter: blur(10px);
      }

      .auth-overlay.active {
        display: flex;
      }

      .auth-loader {
        text-align: center;
      }

      .loader-ring {
        width: 100px;
        height: 100px;
        border: 4px solid rgba(0, 217, 255, 0.1);
        border-top: 4px solid var(--quantum-blue);
        border-radius: 50%;
        animation: spin 1s linear infinite;
        margin: 0 auto 20px;
        box-shadow: 0 0 30px var(--quantum-blue);
      }

      .loader-text {
        color: var(--quantum-blue);
        font-size: 1.2rem;
        letter-spacing: 3px;
        text-transform: uppercase;
        animation: textPulse 1.5s ease-in-out infinite;
      }

      @keyframes textPulse {
        0%,
        100% {
          opacity: 1;
        }
        50% {
          opacity: 0.5;
        }
      }

      /* Transition animation */
      .page-transition {
        position: fixed;
        inset: 0;
        background: linear-gradient(135deg, var(--quantum-blue), var(--cyber-purple));
        z-index: 10000;
        display: none;
        align-items: center;
        justify-content: center;
        opacity: 0;
      }

      .page-transition.active {
        display: flex;
        animation: transitionFade 1.5s ease-in-out forwards;
      }

      @keyframes transitionFade {
        0% {
          opacity: 0;
        }
        50% {
          opacity: 1;
        }
        100% {
          opacity: 0;
        }
      }

      .transition-content {
        text-align: center;
        color: #ffffff;
      }

      .transition-logo {
        width: 120px;
        height: 120px;
        margin: 0 auto 30px;
        animation: logoExpand 1.5s ease-in-out;
      }

      @keyframes logoExpand {
        0%,
        100% {
          transform: scale(1);
        }
        50% {
          transform: scale(1.3);
        }
      }

      .transition-text {
        font-size: 2rem;
        letter-spacing: 4px;
        text-transform: uppercase;
        animation: textGlow 1.5s ease-in-out infinite;
      }

      @keyframes textGlow {
        0%,
        100% {
          text-shadow: 0 0 20px rgba(255, 255, 255, 0.5);
        }
        50% {
          text-shadow: 0 0 40px rgba(255, 255, 255, 0.8);
        }
      }

      /* Responsive design */
      @media (max-width: 600px) {
        .quantum-content {
          padding: 0 24px 32px;
        }

        .quantum-header {
          padding: 32px 24px 16px;
        }

        .quantum-title {
          font-size: 2rem;
          letter-spacing: 2px;
        }

        .quantum-logo {
          width: 60px;
          height: 60px;
        }

        .neon-form input {
          padding: 14px 16px;
          font-size: 14px;
        }

        #loginBtn {
          padding: 16px 20px;
          font-size: 14px;
        }

        .orb {
          display: none;
        }
      }

      @media (max-width: 400px) {
        .quantum-title {
          font-size: 1.6rem;
        }

        .quantum-options {
          flex-direction: column;
          gap: 12px;
          align-items: flex-start;
        }
      }

      /* Accessibility */
      @media (prefers-reduced-motion: reduce) {
        *,
        *::before,
        *::after {
          animation-duration: 0.01ms !important;
          animation-iteration-count: 1 !important;
          transition-duration: 0.01ms !important;
        }
      }

      /* Performance optimization */
      .quantum-card,
      .neon-form input,
      #loginBtn {
        will-change: transform;
      }

      /* Focus visible for accessibility */
      *:focus-visible {
        outline: 2px solid var(--quantum-blue);
        outline-offset: 4px;
      }
    </style>
  </head>
  <body>
    <!-- Background layers -->
    <div class="starfield"></div>
    <canvas id="particleCanvas"></canvas>
    <div class="neural-grid"></div>
    <div class="scan-effect"></div>

    <!-- Floating orbs -->
    <div class="orb orb-1"></div>
    <div class="orb orb-2"></div>
    <div class="orb orb-3"></div>

    <!-- Main login container -->
    <div class="login-container">
      <div class="quantum-card">
        <div class="quantum-header">
          <div class="quantum-logo">
            <div class="logo-ring"></div>
            <div class="logo-ring"></div>
            <div class="logo-ring"></div>
            <div class="logo-core"></div>
          </div>
          <h1 class="quantum-title">NIJJARA</h1>
          <p class="quantum-subtitle">For Contracting & Woodcrafts</p>
          <div class="access-code">QUANTUM ACCESS PORTAL</div>
        </div>

        <div class="quantum-content">
          <form class="neon-form" id="loginForm" novalidate>
            <div class="input-group">
              <div class="holo-input-wrapper">
                <div class="holo-corners">
                  <div class="corner corner-tl"></div>
                  <div class="corner corner-tr"></div>
                  <div class="corner corner-bl"></div>
                  <div class="corner corner-br"></div>
                </div>
                <label for="username">↠ NEURAL ID</label>
                <input
                  type="text"
                  id="username"
                  name="username"
                  required
                  autocomplete="username"
                  placeholder="Enter quantum identity"
                  aria-label="Username"
                />
                <div class="scan-line-input"></div>
                <div class="input-particles">
                  <div class="particle" style="--tx: 20px; --ty: -30px; animation-delay: 0s"></div>
                  <div
                    class="particle"
                    style="--tx: -25px; --ty: -25px; animation-delay: 0.3s"
                  ></div>
                  <div
                    class="particle"
                    style="--tx: 30px; --ty: -20px; animation-delay: 0.6s"
                  ></div>
                </div>
              </div>
            </div>

            <div class="input-group">
              <div class="holo-input-wrapper">
                <div class="holo-corners">
                  <div class="corner corner-tl"></div>
                  <div class="corner corner-tr"></div>
                  <div class="corner corner-bl"></div>
                  <div class="corner corner-br"></div>
                </div>
                <label for="password">↠ ACCESS CODE</label>
                <input
                  type="password"
                  id="password"
                  name="password"
                  required
                  autocomplete="current-password"
                  placeholder="Enter security cipher"
                  aria-label="Password"
                />
                <div class="scan-line-input"></div>
                <div class="input-particles">
                  <div class="particle" style="--tx: -20px; --ty: -30px; animation-delay: 0s"></div>
                  <div
                    class="particle"
                    style="--tx: 25px; --ty: -25px; animation-delay: 0.3s"
                  ></div>
                  <div
                    class="particle"
                    style="--tx: -30px; --ty: -20px; animation-delay: 0.6s"
                  ></div>
                </div>
              </div>
            </div>

            <div class="quantum-options">
              <label class="holo-checkbox">
                <input type="checkbox" id="remember" name="remember" />
                <span>◈ PERSIST SESSION</span>
              </label>
              <a href="#" onclick="showForgotPassword(); return false;">RECOVERY MODE</a>
            </div>

            <div class="quantum-button-wrapper">
              <button type="submit" id="loginBtn">
                <span class="button-text">INITIATE CONNECTION</span>
                <div class="button-spinner"></div>
              </button>
            </div>

            <div class="message error" id="errorMsg" role="alert"></div>
            <div class="message success" id="successMsg" role="status"></div>
          </form>
        </div>
      </div>

      <p class="quantum-footer">⟨ SECURE QUANTUM LINK ESTABLISHED ⟩</p>
    </div>

    <!-- Loading overlay -->
    <div class="auth-overlay" id="authOverlay">
      <div class="auth-loader">
        <div class="loader-ring"></div>
        <div class="loader-text">AUTHENTICATING...</div>
      </div>
    </div>

    <!-- Transition overlay -->
    <div class="page-transition" id="pageTransition">
      <div class="transition-content">
        <div class="transition-logo">
          <div class="logo-ring"></div>
          <div class="logo-ring"></div>
          <div class="logo-ring"></div>
          <div class="logo-core"></div>
        </div>
        <div class="transition-text">ACCESS GRANTED</div>
      </div>
    </div>

    <script>
      // ========== PARTICLE SYSTEM ==========
      const canvas = document.getElementById('particleCanvas');
      const ctx = canvas.getContext('2d');

      canvas.width = window.innerWidth;
      canvas.height = window.innerHeight;

      window.addEventListener('resize', () => {
        canvas.width = window.innerWidth;
        canvas.height = window.innerHeight;
      });

      class Particle {
        constructor() {
          this.reset();
        }

        reset() {
          this.x = Math.random() * canvas.width;
          this.y = Math.random() * canvas.height;
          this.vx = (Math.random() - 0.5) * 0.5;
          this.vy = (Math.random() - 0.5) * 0.5;
          this.size = Math.random() * 2 + 1;
          this.opacity = Math.random() * 0.5 + 0.2;
          this.hue = Math.random() * 60 + 180; // Blue to cyan range
        }

        update() {
          this.x += this.vx;
          this.y += this.vy;

          if (this.x < 0 || this.x > canvas.width || this.y < 0 || this.y > canvas.height) {
            this.reset();
          }
        }

        draw() {
          ctx.beginPath();
          ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
          ctx.fillStyle = `hsla(${this.hue}, 100%, 60%, ${this.opacity})`;
          ctx.shadowBlur = 10;
          ctx.shadowColor = `hsla(${this.hue}, 100%, 60%, ${this.opacity})`;
          ctx.fill();
        }
      }

      const particles = [];
      const particleCount = window.innerWidth < 768 ? 50 : 100;

      for (let i = 0; i < particleCount; i++) {
        particles.push(new Particle());
      }

      function animateParticles() {
        ctx.clearRect(0, 0, canvas.width, canvas.height);

        particles.forEach((particle) => {
          particle.update();
          particle.draw();
        });

        // Draw connections
        particles.forEach((p1, i) => {
          particles.slice(i + 1).forEach((p2) => {
            const dx = p1.x - p2.x;
            const dy = p1.y - p2.y;
            const distance = Math.sqrt(dx * dx + dy * dy);

            if (distance < 150) {
              ctx.beginPath();
              ctx.moveTo(p1.x, p1.y);
              ctx.lineTo(p2.x, p2.y);
              ctx.strokeStyle = `rgba(0, 217, 255, ${0.1 * (1 - distance / 150)})`;
              ctx.lineWidth = 0.5;
              ctx.stroke();
            }
          });
        });

        requestAnimationFrame(animateParticles);
      }

      animateParticles();

      // ========== FORM HANDLING ==========
      const form = document.getElementById('loginForm');
      const btn = document.getElementById('loginBtn');
      const errorMsg = document.getElementById('errorMsg');
      const successMsg = document.getElementById('successMsg');
      const usernameInput = document.getElementById('username');
      const passwordInput = document.getElementById('password');
      const authOverlay = document.getElementById('authOverlay');
      const pageTransition = document.getElementById('pageTransition');

      // Enhanced input field interactions
      const inputs = document.querySelectorAll(
        '.neon-form input[type="text"], .neon-form input[type="password"]'
      );

      inputs.forEach((input) => {
        // Focus effects
        input.addEventListener('focus', function () {
          this.parentElement.classList.add('focused');
          this.placeholder = '';

          // Add ripple effect
          const wrapper = this.parentElement;
          wrapper.style.transform = 'scale(1.01)';
        });

        input.addEventListener('blur', function () {
          this.parentElement.classList.remove('focused');
          const wrapper = this.parentElement;
          wrapper.style.transform = 'scale(1)';

          // Restore placeholder
          if (this.value === '') {
            if (this.id === 'username') {
              this.placeholder = 'Enter quantum identity';
            } else {
              this.placeholder = 'Enter security cipher';
            }
          }
        });

        // Real-time validation
        input.addEventListener('input', function () {
          if (this.value.length > 0) {
            this.style.borderColor = 'var(--plasma-teal)';
          } else {
            this.style.borderColor = '';
          }
        });

        // Keyboard navigation
        input.addEventListener('keydown', function (e) {
          if (e.key === 'Enter') {
            e.preventDefault();
            if (this.id === 'username' && this.value.trim()) {
              passwordInput.focus();
            } else if (this.id === 'password' && this.value) {
              form.dispatchEvent(new Event('submit'));
            }
          }
        });
      });

      // Form submission
      form.addEventListener('submit', function (e) {
        e.preventDefault();

        const username = usernameInput.value.trim();
        const password = passwordInput.value;

        // Validation with futuristic messages
        if (!username && !password) {
          showError('⚠ NEURAL ID AND ACCESS CODE REQUIRED');
          usernameInput.focus();
          return;
        }

        if (!username) {
          showError('⚠ NEURAL ID REQUIRED');
          usernameInput.focus();
          return;
        }

        if (!password) {
          showError('⚠ ACCESS CODE REQUIRED');
          passwordInput.focus();
          return;
        }

        // Show loading state
        btn.disabled = true;
        btn.classList.add('loading');
        hideMessages();
        authOverlay.classList.add('active');

        // Call authentication service
        google.script.run
          .withSuccessHandler(handleSuccess)
          .withFailureHandler(handleFailure)
          .authenticateUser(username, password);
      });

      function handleSuccess(response) {
        // Remove loading state
        btn.disabled = false;
        btn.classList.remove('loading');
        authOverlay.classList.remove('active');

        if (response && response.success) {
          showSuccess('✓ QUANTUM AUTHENTICATION SUCCESSFUL');

          // Store session data
          try {
            sessionStorage.setItem('nijjara_session', JSON.stringify(response.data));
            sessionStorage.setItem('nijjara_login_success', 'true');

            // Show transition animation
            setTimeout(() => {
              pageTransition.classList.add('active');

              // Navigate after animation
              setTimeout(() => {
                const baseUrl = window.location.origin + window.location.pathname;
                const sessionToken = encodeURIComponent(response.data.sessionToken || '');
                window.location.href = baseUrl + '?page=dashboard&token=' + sessionToken;
              }, 1500);
            }, 800);
          } catch (err) {
            showError('⚠ SESSION INITIALIZATION FAILED');
            console.error('Session storage error:', err);
          }
        } else {
          // Handle authentication errors
          let errorMessage = '⚠ AUTHENTICATION FAILED';

          if (response && response.message) {
            errorMessage = '⚠ ' + response.message.toUpperCase();
          } else if (response && response.errors) {
            if (response.errors.includes('RATE_LIMIT_EXCEEDED')) {
              errorMessage = '⚠ RATE LIMIT EXCEEDED - RETRY LATER';
            } else if (response.errors.includes('ACCOUNT_INACTIVE')) {
              errorMessage = '⚠ ACCOUNT INACTIVE - CONTACT ADMIN';
            } else {
              errorMessage = '⚠ INVALID CREDENTIALS';
            }
          } else {
            errorMessage = '⚠ INVALID NEURAL ID OR ACCESS CODE';
          }

          showError(errorMessage);
          passwordInput.value = '';
          passwordInput.focus();

          // Shake animation
          form.style.animation = 'shake 0.5s ease-in-out';
          setTimeout(() => {
            form.style.animation = '';
          }, 500);
        }
      }

      function handleFailure(error) {
        // Remove loading state
        btn.disabled = false;
        btn.classList.remove('loading');
        authOverlay.classList.remove('active');

        // Enhanced error handling
        let errorMessage = '⚠ CONNECTION FAILED';

        if (error && error.message) {
          if (error.message.includes('timeout')) {
            errorMessage = '⚠ CONNECTION TIMEOUT';
          } else if (error.message.includes('network')) {
            errorMessage = '⚠ NETWORK ERROR';
          } else {
            errorMessage = '⚠ ' + error.message.toUpperCase();
          }
        }

        showError(errorMessage);

        // Shake animation
        form.style.animation = 'shake 0.5s ease-in-out';
        setTimeout(() => {
          form.style.animation = '';
        }, 500);
      }

      function showError(msg) {
        errorMsg.textContent = msg;
        errorMsg.classList.add('show');
        successMsg.classList.remove('show');

        // Auto-hide after 5 seconds
        setTimeout(() => {
          errorMsg.classList.remove('show');
        }, 5000);
      }

      function showSuccess(msg) {
        successMsg.textContent = msg;
        successMsg.classList.add('show');
        errorMsg.classList.remove('show');
      }

      function hideMessages() {
        errorMsg.classList.remove('show');
        successMsg.classList.remove('show');
      }

      function showForgotPassword() {
        showError('⚠ PASSWORD RECOVERY MODULE COMING SOON');
      }

      // Add shake animation
      const style = document.createElement('style');
      style.textContent = `
        @keyframes shake {
          0%, 100% { transform: translateX(0); }
          10%, 30%, 50%, 70%, 90% { transform: translateX(-8px); }
          20%, 40%, 60%, 80% { transform: translateX(8px); }
        }
      `;
      document.head.appendChild(style);

      // Auto-focus username field
      window.addEventListener('load', function () {
        setTimeout(() => {
          usernameInput.focus();
        }, 100);
      });

      // Mouse tracking effect for card
      const quantumCard = document.querySelector('.quantum-card');
      let mouseX = 0;
      let mouseY = 0;
      let cardX = 0;
      let cardY = 0;

      document.addEventListener('mousemove', (e) => {
        mouseX = e.clientX / window.innerWidth - 0.5;
        mouseY = e.clientY / window.innerHeight - 0.5;
      });

      function animateCard() {
        cardX += (mouseX * 10 - cardX) * 0.1;
        cardY += (mouseY * 10 - cardY) * 0.1;

        if (quantumCard && window.innerWidth > 768) {
          quantumCard.style.transform = `perspective(1000px) rotateY(${cardX}deg) rotateX(${-cardY}deg)`;
        }

        requestAnimationFrame(animateCard);
      }

      if (window.innerWidth > 768) {
        animateCard();
      }

      // Performance monitoring
      console.log(
        '%c🚀 NIJJARA QUANTUM PORTAL INITIALIZED',
        'color: #00d9ff; font-size: 16px; font-weight: bold;'
      );
      console.log('%c⚡ Performance Mode: Active', 'color: #b030ff; font-size: 12px;');
    </script>
  </body>
</html>
